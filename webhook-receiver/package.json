{
  "name": "kye-webhook-receiver-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reference KYE Protocol webhook receiver — Express + @kye/webhook-verify. Validates timestamp window, signature, body↔header event_id, and de-dups on KYE-Delivery-ID. (Signature algorithm is part of the patent track and not disclosed here; the verifier helper ships in the SDK.)",
  "main": "receiver.js",
  "scripts": {
    "build": "tsc",
    "start": "node receiver.js"
  },
  "dependencies": {
    "@kye/webhook-dispatcher": "workspace:*",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
